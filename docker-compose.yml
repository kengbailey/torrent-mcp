services:
  torrent-mcp:
    container_name: torrent-mcp
    build: .
    ports:
      - "${MCP_PORT:-6735}:${MCP_PORT:-6735}"
    env_file: .env
    environment:
      - MCP_HOST=0.0.0.0
    restart: unless-stopped
